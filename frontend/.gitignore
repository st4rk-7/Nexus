# Frontend dependencies & build output — regenerable, never commit.
node_modules/
dist/

# npm lockfile — byproduct of installing with npm (Bun install hangs on this
# machine). package.json is the source of truth.
package-lock.json

# Vite/editor cruft
.vite/
*.local
